# Lab 01 — raft node 2 (joins node 1)
if [ -f /etc/bash.bashrc ]; then
  # shellcheck source=/dev/null
  . /etc/bash.bashrc
fi

export PS1='[vault-node-2] \u@\h:\w\$ '
export VAULT_ADDR=http://vault-node-2:8200
unset VAULT_TOKEN 2>/dev/null || true

printf '\n=== vault-node-2 (127.0.0.3:8200 / vault-node-2:8200) — start: vault server -config=/tmp/vault-node-2.hcl ===\n\n'
